<!DOCTYPE html>
<%- include('partials/head.ejs') %>
<body>
    <div class="form">
        <form action="/auth/login" method="post">
            <h3><%= title %></h3>
            <div>
                <input
                    name="client_id"
                    type="hidden"
                    value="<%= query.client_id %>"
                >
                <input
                    name="redirect_uri"
                    type="hidden"
                    value="<%= query.redirect_uri %>"
                >
                <input
                    name="response_type"
                    type="hidden"
                    value="<%= query.response_type %>"
                >
                <input
                    name="grant_type"
                    type="hidden"
                    value="password"
                >
            </div>
            <div class="form-group">
                <input
                    class="form-control item"
                    id="username"
                    name="username"
                    placeholder="Email"
                    required
                    type="email"
                    value="<%= query.username %>"
                >
            </div>
            <div class="form-group">
                <input
                    class="form-control item"
                    id="password"
                    minlength="6"
                    name="password"
                    placeholder="Пароль"
                    required
                    type="password"
                    value="<%= query.password %>"
                >
            </div>
            <%- include('partials/errors.ejs') %>
            <%- include('partials/button.ejs', {button: 'Войти'}) %>
        </form>
        <%- include('partials/strategies.ejs') %>
        <div class="bottom">
            <p>
                Нет аккаунта?
                <a class="link" href="/auth/register.html">Зарегистрироваться</a>
            </p>
            <p>
                Забыли пароль?
                <a class="link" href="/auth/restore.html">Сбросить</a>
            </p>
        </div>
    </div>
</body>
</html>